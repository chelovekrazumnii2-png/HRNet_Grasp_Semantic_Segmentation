# Core
# NOTE: install torch + torchvision SEPARATELY with the right CUDA index URL
# *before* running this requirements file, e.g. for CUDA 12.6:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Plain `pip install torch` from PyPI yields a CPU-only build. See
# https://pytorch.org/get-started/locally/ for the exact command for your
# driver / CUDA combination.
timm>=0.9.12

# Data / IO
numpy>=1.23
opencv-python>=4.7
Pillow>=9.4
scikit-image>=0.20
tifffile>=2023.4
imagecodecs>=2023.7   # required by tifffile to decode LZW-compressed Jacquard depth TIFFs
imageio>=2.28

# Utilities
pyyaml>=6.0
tqdm>=4.65
tensorboard>=2.13

# Visualisation (used by grasp_seg/viz/, tools/visualize.py, notebooks/visualize.ipynb)
matplotlib>=3.6
pandas>=2.0
scipy>=1.10        # gaussian_filter / maximum_filter for the grasp-rectangle decoder
seaborn>=0.12      # nicer style for report-quality plots (optional but light-weight)
